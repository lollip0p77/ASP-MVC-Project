@model ASP_MVC_SitePandas.Models.AllergiesEnfant

@{
    ViewData["Title"] = "Edit";
}

<section id="AllergieEnfant" class="padd-section">

    <div class="container" data-aos="fade-up">
        <div class="section-title text-center">
            <h2>Allergie de l'enfant'</h2>
            <p class="" style="text-align: center">Veuillez remplir les champs ci dessous pour modifier une allergie pour l'enfant.</p>
        </div>

        <div class="row justify-content-center" data-aos="fade-up" data-aos-delay="100">


            <div class="col-lg-5 col-md-8">

                <div class="form">
                    <form asp-action="Edit">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <input type="hidden" asp-for="Id" />
                        <div class="form-group" style="padding-top: 10px;">
                            <select asp-for="EnfantId" class="form-control" asp-items="ViewBag.EnfantId"></select>
                        </div>
                        <div class="form-group" style="padding-top: 10px;">
                            <select id="test" asp-for="AllergieId" class="form-control" asp-items="ViewBag.AllergieId"></select>
                        </div>
                        <div class="form-group">
                            <textarea asp-for="Intervention" class="form-control" rows="5" placeholder="Intervention" required></textarea>
                            @*<span asp-validation-for="Description" class="text-danger"></span>*@
                        </div>
                        <div style="padding-top: 20px" class="text-center"><button class="btn" style="background-color:limegreen" type="submit" value="Save">Modifier</button></div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
